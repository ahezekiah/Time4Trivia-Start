extends layout

block content
  .container
    h1 Users
    div(class="userContainer")
      each user in users 
        div(class='userBox')
          h2 Username: #{user.Username}
          div Password: #{user.Password}
          div
            strong Role:
            if user.Role
              span(class="role-badge role-" + user.Role)= user.Role.toUpperCase()
            else
              span Unknown Role 


          div
            if user.Enabled === 1
              span(class='disabled-label') Account Disabled
              a(href='/a/enable/' + user.UserId) Enable
            else
              a(href='/a/disable/' + user.UserId) Disable

          div
            a(href='/a/delete/' + user.UserId) Remove

          div
            if user.Role === 'admin'
              if user.Role !== 'admin'
                a(href='/a/promote/' + user.UserId) Promote
              if user.Role !== 'user'
                a(href='/a/demote/' + user.UserId) Demote
                //this is wrong, it gets from the users Role, not the ACTUAL user logged in fix later, solve why user.UserID not returning id


